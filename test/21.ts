//expret this and import in an file
export default interface Str {
    name:string,
    age:number,
    email:string
}

//can be import like other react comp or module
var userinfo:Str = {
    name:'name1',
    age:21,
    email:"email1"
}